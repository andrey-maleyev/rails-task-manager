<h1>My tasks</h1>
<ul style="list-style-type: none">
  <% @tasks.each do |task| %>
    <li>
      <input type="checkbox"
        <%= "checked" if task.completed %>
      />
      <%= link_to task.title, task_path(id: task.id) %>
    </li>
  <% end %>
</ul>