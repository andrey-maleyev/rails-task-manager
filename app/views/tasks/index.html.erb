<div class="container">
	<h1>My tasks</h1>
	<ul style="list-style-type: none">
	  <% @tasks.each do |task| %>
	    <li>
	      <input type="checkbox"
	        <%= "checked" if task.completed %>
	      />
	      <%= task.title %>
	    </li>
	  <% end %>
	</ul>
</div>

